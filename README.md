## Running the Project Locally

1. Install and open XAMPP.
2. Start the Apache server.
3. Place the project folder named " xampp\htdocs\collection_manager" inside your XAMPP htdocs directory.
  Open the application at:
  http://localhost/collection_manager/

  
Netlify URL: https://tranquil-pony-bfb366.netlify.app/
Backend Language: PHP
JSON persistance is when we save our application data in JSON format to a persistent storage location (a file), which ensures the information survives after the application is closed / restarted.

the practice of saving application data in JSON (JavaScript Object Notation) format to a persistent storage medium—such as a file, database, or cloud storage—ensuring the information survives after an application closes or a device restarts.
